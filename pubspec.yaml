name: cruise_ships
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  json_annotation: ^4.4.0
  dio: ^4.0.6
  freezed: ^1.1.0 # Later versions have conflict with bloc_test
  flutter_bloc: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter  
  flutter_lints: ^1.0.0
  json_serializable: ^6.1.5
  build_runner: ^2.1.8
  bloc_test: ^9.0.3

flutter:  
  uses-material-design: true
